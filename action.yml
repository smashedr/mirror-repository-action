name: "Mirror Repository Action"
description: "Mirror Repository Action."
author: "Shane"
branding:
  icon: "git-merge"
  color: "black"

inputs:
  url:
    description: "Remote Repository URL"
    required: false
  host:
    description: "Remote Repository Host"
    required: false
  owner:
    description: "Remote Repository Owner"
    required: false
  repo:
    description: "Remote Repository Name"
    required: false
  username:
    description: "Remote Repository Username"
    required: false
  password:
    description: "Remote Repository Password"
    required: false

#outputs:
#  time:
#    description: "Your output description here"

runs:
  using: "docker"
  image: "Dockerfile"
